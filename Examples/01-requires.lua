 -- This line will include the file utils.lua, which must be present in
 -- the same folder as the current script
require "utils"

-- Functions from that utils.lua filed can now be called
Equip("torch")